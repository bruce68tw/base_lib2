import 'dart:io';
//import 'package:video_player/video_player.dart';

//static class, cannot use _Fun
class VideoUt {

  /*
  //initial
  static Future initAsync(VideoPlayerController? ctrl, String path, Function fnOk) async {
    ctrl = VideoPlayerController.file(File(path));
    await ctrl.initialize();
    await ctrl.setLooping(false);
    fnOk();
    //return ctrl;
  }
  */
  
} //class
